# Neovim notes (kickstart.nvim)

This dotfiles repo assumes Neovim config is managed via kickstart.nvim.

## Install kickstart.nvim

```bash
git clone https://github.com/Charan-Patnaik/kickstart.nvim ~/.config/nvim