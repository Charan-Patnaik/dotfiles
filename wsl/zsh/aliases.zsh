# General
alias ll='ls -lah'
alias la='ls -A'
alias cls='clear'

# Git
alias gs='git status'
alias ga='git add'
alias gc='git commit'
alias gp='git push'
alias gl='git log --oneline --graph --decorate -20'

# tmux
alias ta='tmux attach -t'
alias tls='tmux ls'
alias tn='tmux new -s'

# Neovim
alias v='nvim'
alias vi='nvim'

# WSL helpers
alias wsl-restart='powershell.exe -Command "wsl --shutdown"'